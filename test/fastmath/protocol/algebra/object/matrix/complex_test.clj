(ns fastmath.protocol.algebra.object.matrix.complex-test
  (:require [fastmath.protocol.algebra.object.matrix.complex :as sut]
            [clojure.test :as t]))

(defn protocol-tests
  "Takes a complex matrix constructor, make, and uses it to check conformity with the protocol."
  [make]
  (let []))

